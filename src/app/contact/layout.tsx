export const metadata = {
  title: 'Contact',
  description: 'Get in touch with PiCode Solutions for custom software development and consultation.',
};

export default function ContactLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
